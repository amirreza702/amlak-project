"use client";

/**
 * =========================================================
 * SearchMap
 * =========================================================
 *
 * نقشه واقعی صفحه Search
 *
 * وظیفه این کامپوننت فعلاً فقط نمایش نقشه است.
 *
 * Markerهای ملک توسط RealMap مدیریت می‌شوند.
 *
 * در این مرحله:
 * - کارت جداگانه‌ای در گوشه صفحه نداریم.
 * - Popup خود Marker روی نقشه باقی می‌ماند.
 *
 * در مراحل بعدی، رفتار انتخاب ملک را توسعه می‌دهیم.
 * =========================================================
 */

import { RealMap } from "@/shared/components/map/RealMap";

/**
 * =========================================================
 * SearchMap
 * =========================================================
 */

export function SearchMap() {
  return (
    <div
      className="
        absolute
        inset-0
        z-0
        overflow-hidden
      "
    >
      {/* ---------------------------------------------------
          نقشه واقعی
      --------------------------------------------------- */}

      <RealMap />

      {/* ---------------------------------------------------
          لایه بسیار ملایم روی نقشه
          
          pointer-events-none باعث می‌شود این لایه مانع
          تعامل کاربر با نقشه نشود.
      --------------------------------------------------- */}

      <div
        className="
          pointer-events-none
          absolute
          inset-0
          z-[1]
          bg-white/5
        "
      />
    </div>
  );
}